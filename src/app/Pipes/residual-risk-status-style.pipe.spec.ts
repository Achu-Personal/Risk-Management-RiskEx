import { ResidualRiskStatusStylePipe } from './residual-risk-status-style.pipe';

describe('ResidualRiskStatusStylePipe', () => {
  it('create an instance', () => {
    const pipe = new ResidualRiskStatusStylePipe();
    expect(pipe).toBeTruthy();
  });
});
